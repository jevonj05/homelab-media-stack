# Homelab Maintenance Scripts

This folder contains helper scripts for maintaining the media stack.

## update-containers.sh

Pulls the latest Docker images, recreates the containers, and removes unused old images.

Usage:

```bash
./scripts/update-containers.sh
